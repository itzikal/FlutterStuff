import 'package:flutter/material.dart';

class DrillsScreen extends StatelessWidget {
  const DrillsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Drills'),);
  }
}
